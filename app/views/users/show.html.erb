<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :login %><br />
    <%= f.text_field :login %>
  </div>
  <div class="field">
    <%= f.label :password, 'New password' %><br />
    <%= f.password_field :password %>
  </div>
  <div class="field">
    <%= label :password, :confirm, 'Confirm new password' %><br />
    <%= password_field :password, :confirm %>
  </div>
  <div class="actions">
    <%= f.submit 'Update settings', :class => 'btn' %>
  </div>
<% end %>
